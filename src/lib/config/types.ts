import { UserConfig, ErrorHandlingConfig, SessionConfig, UIConfig, FallbackAgentsConfig } from './schema';

export interface RalphConfig extends UserConfig {}

export { ErrorHandlingConfig, SessionConfig, UIConfig, FallbackAgentsConfig };
