{
  "name": "opencode-ralph",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "ralph": "./bin/ralph.ts"
  },
  "scripts": {
    "dev": "bun run --conditions=browser src/index.ts",
    "build": "bun run scripts/build.ts",
    "postbuild": "bun run scripts/install.ts",
    "typecheck": "bun x tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "@opencode-ai/sdk": "1.1.1",
    "@opentui/core": "0.1.68",
    "@opentui/solid": "0.1.68",
    "solid-js": "^1.9.0",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/yargs": "^17.0.35",
    "typescript": "^5.0.0"
  }
}
